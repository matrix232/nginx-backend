services:
  backend:
    build:
      context: ./backend
    networks:
      - app-network

  nginx:
    image: nginx:1.30.1
    depends_on:
      - backend
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - app-network

networks:
  app-network:
    driver: bridge